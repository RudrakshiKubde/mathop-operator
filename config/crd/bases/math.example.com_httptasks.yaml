---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: httptasks.math.example.com
spec:
  group: math.example.com
  names:
    kind: HTTPTask
    listKind: HTTPTaskList
    plural: httptasks
    singular: httptask
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    - jsonPath: .status.statusCode
      name: StatusCode
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              endpoint:
                description: Endpoint is the full URL to call, e.g. "http://localhost:9090/add"
                type: string
              headers:
                additionalProperties:
                  type: string
                type: object
              input:
                description: |-
                  Input is the static/base JSON request body. Fields from InputFrom are
                  merged into a copy of this before the request is sent.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              inputFrom:
                description: |-
                  InputFrom maps fields from other HTTPTasks' outputs into this Task's
                  request body. A Task with no InputFrom entries is a "root" task.
                items:
                  description: |-
                    FieldMapping pulls one field out of another Task's output JSON and places
                    it into this Task's request body before the HTTP call is made.
                  properties:
                    sourceField:
                      description: SourceField is a dot-path into the source Task's
                        status.output, e.g. "sum" or "data.result"
                      type: string
                    sourceRef:
                      description: SourceReference points at another HTTPTask by name.
                      properties:
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                    targetField:
                      description: TargetField is a dot-path into THIS task's request
                        body, e.g. "value" or "a.b"
                      type: string
                  required:
                  - sourceField
                  - sourceRef
                  - targetField
                  type: object
                type: array
              method:
                description: Method defaults to POST if empty
                type: string
            required:
            - endpoint
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedInputHash:
                description: |-
                  ObservedInputHash records a hash of the last request body actually
                  sent, so we don't re-call the endpoint when nothing has changed.
                type: string
              output:
                description: Output is the raw JSON response body from the HTTP call
                type: object
                x-kubernetes-preserve-unknown-fields: true
              statusCode:
                description: StatusCode of the HTTP response
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
